{
  "name": "research-os",
  "version": "0.1.0",
  "private": true,
  "description": "ResearchOS — modular autonomous research platform. Pi + pi-mesh as first runtime, research core harness independent.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck"
  }
}
